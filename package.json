{
  "name": "seiswarm",
  "version": "0.1.0",
  "private": true,
  "description": "On-Chain AI Agent Swarm Platform on Sei",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
